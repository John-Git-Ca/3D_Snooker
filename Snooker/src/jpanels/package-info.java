/**
 * Contains abtract subclasses for the JPanel.
 * A JPanel is basically a container for
 * anything you want to place inside a window.
 * <p>
 * Make all additional subclasses extend the
 * {@link jpanels.BasicView} instead of the JPanel, for polymorphism sakes.
 */
package jpanels;
